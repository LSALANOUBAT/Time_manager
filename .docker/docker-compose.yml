version: '3.8'
services:
  vue:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: time_manager_vue
    ports:
      - "8080:8080"
    volumes:
      - ../frontend:/app
    networks:
      - traefik_routing
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vue.rule=Host(`vue.orbesle.fr`)" # Replace with your actual domain
      - "traefik.http.routers.vue.entrypoints=websecure"
      - "traefik.http.routers.vue.tls.certresolver=myresolver"
      - "traefik.docker.network=traefik_routing"

  db:
    image: postgres:14-alpine
    container_name: time_manager_db
    environment:
      POSTGRES_USER: gotham_admin
      POSTGRES_PASSWORD: gotham_password
      POSTGRES_DB: time_manager
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - traefik_routing
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gotham_admin"]
      interval: 10s
      retries: 5

  web:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: time_manager_web
    depends_on:
      db:
        condition: service_healthy
    environment:
      MIX_ENV: dev
      DATABASE_URL: "ecto://gotham_admin:gotham_password@db/time_manager"
    ports:
      - "4000:4000"
    volumes:
      - ../time_manager:/time_manager
    networks:
      - traefik_routing
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web.rule=Host(`web.orbesle.fr`)" # Replace with your actual domain
      - "traefik.http.routers.web.entrypoints=websecure"
      - "traefik.http.routers.web.tls.certresolver=myresolver"
      - "traefik.docker.network=traefik_routing"

volumes:
  pgdata:


networks:
  traefik_routing:
    external: true
    name: reverse-proxy-docker-traefik_routing